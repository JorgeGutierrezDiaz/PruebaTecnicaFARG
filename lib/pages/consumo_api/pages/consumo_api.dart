import 'package:flutter/material.dart';

class ConsumoApiScreen extends StatelessWidget {
  const ConsumoApiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
